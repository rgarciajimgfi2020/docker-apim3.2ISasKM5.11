[server]
hostname = "51.124.232.89"
node_ip = "51.124.232.89"
base_path = "https://$ref{server.hostname}:${carbon.management.port}"
offset=1

[super_admin]
username = "admin"
password = "admin"
create_admin_account = true

#Se comenta esta parte debido a que es una BD de LDAP en H2.
#[user_store]
#type = "read_write_ldap_unique_id"
#connection_url = "ldap://localhost:${Ports.EmbeddedLDAP.LDAPServerPort}"
#connection_name = "uid=admin,ou=system"
#connection_password = "$env{PASS_IS}"
#base_dn = "dc=wso2,dc=org"      #refers the base dn on which the user and group search bases will be generated
#Esto se habilita para que use la bd database.shared_db, la cual es compartida en ambos nodos para la replicación de usuarios.

#CUCUYOYE
#[user_store]
#type = "read_write_ldap_unique_id"
#connection_url = "ldap://localhost:${Ports.EmbeddedLDAP.LDAPServerPort}"
#connection_name = "uid=admin,ou=system"
#connection_password = "admin"
#base_dn = "dc=wso2,dc=org"      #refers the base dn on which the user and group search bases will be generated

[user_store]
type = "database_unique_id"   #refers the base dn on which the user and group search bases will be generated

[database.identity_db]
url = "jdbc:mysql://mysql:3306/WSO2IS_IDENTITY_DB?autoReconnect=true&amp;useSSL=false"
username = "wso2carbon"
password = "wso2carbon"
driver = "com.mysql.cj.jdbc.Driver"

[database.identity_db.pool_options]
maxActive = "80"
maxWait = "60000"
minIdle = "5"
testOnBorrow = true
validationQuery="SELECT 1"
validationInterval="30000"
defaultAutoCommit="false"
rollbackOnReturn="true"

[database.shared_db]
url = "jdbc:mysql://mysql:3306/WSO2AM_SHARED_IS?autoReconnect=true&amp;useSSL=false"
username = "wso2carbon"
password = "wso2carbon"
driver = "com.mysql.cj.jdbc.Driver"

[database.shared_db.pool_options]
maxActive = "80"
maxWait = "60000"
minIdle = "5"
testOnBorrow = true
validationQuery="SELECT 1"
validationInterval="30000"
defaultAutoCommit="false"
rollbackOnReturn="true"

[keystore.primary]
file_name = "wso2carbon.jks"
password = "wso2carbon"

